apiVersion: v1
kind: Service
metadata:
  labels:
    service-name: blog
  name: blog
spec:
  type: LoadBalancer
  ports:
    - name: "80"
      nodePort: {{ .Values.deployment.port }}
      port: 80
      targetPort: 80
  selector:
    service-name: blog
status:
  loadBalancer: {}
