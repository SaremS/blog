name: Deploy kafka middleware
on:
  workflow_dispatch:
  push:
    branches:
    - master
    paths:
    - 'services/kafka-middleware/**' 
    - '.github/workflows/deploy_kafka_middleware.yml'


jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'

    steps:
      - uses: actions/checkout@v2.5.0 # Checking out the repo

      - name: Build and Publish latest Docker image
        uses: VaultVulp/gp-docker-action@1.5.0
        with:
          github-token: ${{ secrets.PUSH_IMAGE_KEY }}     
          image-name: kafka-middleware
          build-context: services/kafka-middleware
          dockerfile: services/kafka-middleware/Dockerfile


  #deploy:
  #  name: Deploy
  #  runs-on: ubuntu-latest
  #  if: github.ref == 'refs/heads/master'
  #  needs: build-and-publish
  #  steps:
  #  - name: executing remote ssh commands using password
  #    uses: appleboy/ssh-action@v0.1.6
  #    with:
  #      host: ${{ secrets.DEPLOY_HOST }}
  #      username: ${{ secrets.DEPLOY_USERNAME }}
  #      password: ${{ secrets.DEPLOY_PASSWORD }}
  #      port: ${{ secrets.DEPLOY_PORT }}
  #      script: |
  #        cd showcases
  #        git pull origin master
  #        docker-compose pull
  #        docker stack up -c docker-compose.yml showcases
